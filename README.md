# Portfolio — Proyecto Contenedor

Este directorio contiene todo el proyecto del portfolio personal. Sigue la estructura de `preferences/workflow.md`:

```
Portfolio/
├── memory/              ← Memoria persistente del proyecto
└── portfolio/           ← Código fuente (React + Vite + Tailwind)
```

## Memoria del proyecto

Ver [`memory/MEMORY.md`](./memory/MEMORY.md) para el índice de decisiones, arquitectura y progreso.

## Despliegue

La configuración de Render vive en [`render.yaml`](./portfolio/render.yaml) y se aplica desde la raíz de este contenedor.